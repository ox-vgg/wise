import React from 'react'
import ReactDOM from 'react-dom/client'
import FacetsApp from './facets/FacetsApp.tsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <FacetsApp />
  </React.StrictMode>,
)
